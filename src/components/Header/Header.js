import HeaderSettings from "./HeaderSettings/HeaderSettings";

const Header = (props) => {
    return ( 
        <header>
            <HeaderSettings/>
        </header>
     );
}
 
export default Header;